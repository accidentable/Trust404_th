# TRUST404 트랙 02 — 프로젝트 명세서

> 이 문서는 Claude Code에 컨텍스트로 넘기기 위한 것입니다.
> 레포 루트에 `CLAUDE.md` 또는 `docs/SPEC.md`로 두고 작업을 시작하세요.
> **결정된 사항은 다시 논의하지 말고 그대로 구현합니다.** 이유는 각 항목에 적혀 있습니다.

---

## 0. 한 줄 요약

알바·행사 스태프·상금 지급 시 요구되는 **신분증 사본을 없애는** DID/VC 프로토타입.
지급자에게는 성명·예금주 일치·만 18세 이상만 제시하고,
주민등록번호는 **국세청 공개키로 봉인**해 지급자가 열지 못한 채 신고서에 첨부한다.

---

## 1. 대회 정보

- 대회: TRUST404 (주최: ANAM145 + 고려대 블록체인 학회 Blockchain Valley)
- 온라인 제출 마감: **2026-09-20 23:59**
- 데모데이: 2026-09-29 (드림플러스 강남)
- 심사: 1차 기술 심사 → 파이널리스트 → 데모데이 심사

### 배점 (전 트랙 공통 100점)

| 항목 | 배점 | 성격 |
|---|---|---|
| 문제 정의 | 25 | 아이디어 |
| 보안적 타당성 | 25 | 트랙 기준이 반영됨 |
| 동작하는 구현 | 20 | 구현 |
| 검증 가능성 | 15 | 트랙 기준이 반영됨 |
| 독창성 | 10 | 아이디어 |
| 확장 가능성 | 5 | 아이디어 |

구현 연동 60 / 순수 아이디어 40. 1차는 코드가 돌아야 살아남고, 2차는 문제 정의와 스토리가 순위를 가른다.

---

## 2. 트랙 02 요구사항 (원문 기준)

**문제 정의**: 자격을 증명하려면 신분증 전체를 넘겨야 하는 사용자. 그리고 최소한의 사실만 확인하면 되는데 과도한 개인정보를 보관하게 되는 서비스 제공자.

**구현 목표**: DID·VC 기반으로 필요한 사실만 증명하는 발급·검증 플로우 구현. 선택적 공개, 영지식 증명, 자격 폐기, 오프체인 프라이버시 보호 등. **ZKP를 직접 구현할 필요는 없음.** 최소 한 개의 동작하는 발급 → 검증 Flow 요구.

**필수 제출물**
- Issuer / Holder / Verifier 역할 구분 (한 화면 안이어도 되지만 역할은 구분되어야 함)
- End-to-End Flow — Credential 발급 → 보관 → 제시 → 검증
- 공개 범위 인터페이스 — 검증자에게 공개되는 정보와 비공개되는 정보를 명확히 구분
- Credential Revocation — 구현 시 평가에 반영

**평가 기준**
- 발급 → 제시 → 검증 중 최소 한 경로가 실제로 동작하는가 (폐기까지 구현 시 가점)
- 어떤 정보가 노출되지 않는지 구체적으로 설명할 수 있는가
- W3C DID/VC 표준 호환 여부 — 필수는 아니나 가점
- 캠퍼스·학회·동아리 생태계의 실재하는 문제를 다루면 「문제 정의」 항목에서 가점

---

## 3. 문제 정의 (발표 기준 문안)

### 핵심 문장

> **세금 한 푼 걷지 않는 하루짜리 알바에도 주민등록증 사본이 카톡으로 오간다.**
> **사장님은 그 번호를 국세청에 넘겨야 할 뿐인데, 원본은 사장님 손에 남는다.**

### 상황

학생이 교내 행사 스태프로 하루 일하고 일당 15만 원을 받는다. 지급자는 원천징수의무자로서 신고 의무가 있어 신분증 사본과 통장 사본을 요구한다. 카톡이나 구글폼으로 받는다.

### 필요한 것 vs 넘어가는 것

| 목적 | 실제 필요한 것 | 누가 필요한가 |
|---|---|---|
| 지급명세서 제출 | 주민등록번호 13자리 | **국세청** |
| 계좌 명의 확인 | "예금주 = 지급 대상자" 참·거짓 | 사장님 |
| 연소자 확인 | "만 18세 이상" 참·거짓 | 사장님 |

사장님 본인이 알아야 할 정보는 하나도 없다. 그런데 실제로 넘어가는 건 주민번호 전체 + 주소 + 사진 + 발급일자가 박힌 이미지 한 장.

### 왜 문제인가 (4가지)

1. **세금이 0원이어도 벌어진다.** 일당 18.7만 원 이하면 원천징수세액 없음. 그런데 지급명세서는 세금이 0원이어도 제출 의무가 있고 미제출 시 가산세. → 걷을 세금이 없는데 주민등록증 사본이 오간다.
2. **사장님도 피해자다.** 주민번호는 법령 근거가 있어야 수집 가능. 개인정보위는 번호가 노출된 신분증 사본 수집 자체를 피하라고 권고. 사장님은 신고하려다 위법 소지와 보관 책임을 동시에 진다.
3. **사본은 실제로 악용된다.** (근거는 §4)
4. **정상 요구와 사기 요구가 똑같이 생겼다.** 사기범도 "급여 지급에 필요하다"며 같은 걸 요구한다. 지원자는 구분할 방법이 없다.

---

## 4. 근거 자료 (발표 슬라이드용)

### 세무 구조 (사실관계)

- 소득 유형별: 사업소득 3.3% / 일용근로소득 (일당−15만원)×약 2.97%, **일당 18.7만원 이하 세금 0원** / 기타소득(상금·원고료) 8.8%
- 원천징수이행상황신고서: 지급일 다음 달 10일. **합계 금액만, 인적사항 없음**
- 지급명세서: **여기에 성명·주민등록번호가 들어감**
  - 사업소득 간이지급명세서 — 지급월 다음 달 말일
  - 일용근로소득 지급명세서 — 지급월 다음 달 말일
  - 기타소득 지급명세서 — 다음 해 2월 말일
- 지급명세서에 주민번호가 없으면 신고 자체가 진행되지 않음 → 뒷자리 마스킹 불가
- 법에 "신분증 사본을 받아라"는 조항은 **없음**. 실무 관행일 뿐

### 사본 악용 사례

- **경실련 피해자 고발대회 (2022.07)**: 휴대폰으로 찍은 운전면허증 사진만으로 본인 인증 통과 → 대출 실행. 유출된 사본이 "대포폰 개통 → 대포통장 개설 → 비대면 대출사기·무단 인출"로 이어짐
- **금감원 구제 신청 (2023.08)**: 유출 사본으로 수천만원 대출, 명의도용 피해자 29명 집단 신청
- **금감원 비대면 금융거래 민원 분석 (2022.12)**: 스마트폰에 저장된 신분증 사진으로 비대면 계좌 개설된 사례. 비대면 금융거래 민원 2017년 415건 → 2021년 1,463건
- **KS한국고용정보 (개인정보위 제재, 2026.04)**: 해킹으로 직원·지원자 4만 875명 개인정보 + **인사서류 약 5만 건 유출**. 그 서류가 주민등록등본·신분증 사본·통장 사본·가족관계증명서. ← **가장 중요한 근거**
- **테무 입점 판매자 (2025.05)**: 마스킹 안 된 신분증 수집 행위로 과징금 4.9억
- **알바 사기 (금감원·알바천국 공유 사례)**: 사기범이 주민번호 확보하려 이력서 요구, 급여계좌 빙자해 계좌·체크카드 요구. 22세 휴학생 피해 사례

### 쓰면 안 되는 근거

- "가상자산 민원 11배 폭증" — 금감원 원문상 증가 원인은 **API 첫 거래 지원금 이벤트 혜택 미지급**. 우리 주제와 무관. 인용하면 역효과

---

## 5. 기존 시도와 한계 (독창성 항목 대비)

| 시도 | 한 것 | 한계 |
|---|---|---|
| 모바일 주민등록증 | 실물과 동일 효력, 제시형 인증 | 은행 모바일 확인서비스는 **사본 보관이 필요한 업무엔 사용 불가**. 제시가 보관 절차를 대체 못 함 |
| 휴대폰 본인확인 | 주민번호 없이 실명확인 | "본인 맞음"만 줌. 지급명세서용 13자리는 못 줌 |
| 캐치시큐 캐치폼 등 | 사본을 **안전하게 받는** 폼 (마스킹·암호화·권한) | 원본은 여전히 존재. 위치만 카톡방→관리 서버로 이동 |
| 마스킹 권고 | 뒷자리 가리기 | 지급명세서엔 13자리 전부 필요 → 이 상황에선 답이 안 됨 |
| EUDI Wallet / SD-JWT | 선택적 공개 (EU 표준, 2027.12부터 규제산업 수용 의무) | **선택적 공개 자체는 우리 발명이 아님.** 국내 세무 절차의 "반드시 전달해야 하는 값" 문제는 다루지 않음 |

**공통 한계**: 주민번호가 지급자 손을 거쳐야 한다는 전제를 아무도 건드리지 않았다.

### 우리 기여 (딱 한 조각)

**국세청만 열 수 있는 봉인 필드.** 지급자를 보관자에서 **전달자**로 되돌린다.

> 발표에서 반드시 정직하게: "선택적 공개는 저희가 만든 게 아닙니다. EU 표준의 기본 기능입니다. 저희가 채운 건 국내 세무 절차에 그걸 끼워 넣을 때 생기는 한 칸입니다."

---

## 6. 역할과 아키텍처

```
주민센터(Issuer) ──VC──▶ 지갑(Holder) ──VP──▶ 사장님(Verifier)
은행(Issuer)     ──VC──▶                          │
                                                  ├─ 봉인 전달 ─▶ 국세청(복호화)
                     폐기 레지스트리(온체인) ◀──조회──┘
```

- **Issuer**: 주민센터(신분 VC), 은행(계좌 VC)
- **Holder**: 나 — 브라우저 지갑, 개인키는 IndexedDB에만
- **Verifier**: 사장님 — 검증 수행 주체. **검증은 사장님이 한다**
- **국세청**: 셋 중 어디도 아님. **봉인 수신자**. 검증이 아니라 복호화만 함

> 국세청이 검증까지 하면 국세청이 "이 사람이 언제 어디서 일했는지"를 알게 되어 구조가 무너진다. 그리고 사장님은 그 자리에서 판단해야 하고, 신고는 다음 달이다.

---

## 7. 데이터 모델

### 신분 VC (주민센터 발급)

```
선택적 공개 가능 (_sd):
  name          "윤태호"
  address       "서울시 ..."
  isOver18      true
  birthDate     "2001-03-14"

항상 포함 (평문):
  statusIndex   4821                 ← 폐기 확인용 인덱스 (난수 배정)
  rrn_sealed    "eyJhbGciOi..."      ← 주민번호를 국세청 공개키로 JWE 암호화
  cnf           { jwk: 홀더 공개키 }   ← holder binding
  iss           "did:key:z6Mk...주민센터"
  exp           만료 시각
  vct           "https://trust404.demo/credentials/resident-id"
```

### 계좌 VC (은행 발급)

```
선택적 공개 가능:
  accountHolderName  "윤태호"
  bankName           "국민은행"
  last4              "4417"

항상 포함:
  cnf, iss, exp, vct, statusIndex
```

→ 두 VC의 성명 일치로 "예금주 일치" 판정. **통장 사본도 사라진다.**

### 제시 요청 (Verifier → Holder)

```json
{
  "verifier": "행사운영팀",
  "purpose": "일당 지급 및 원천징수 신고",
  "requested": ["name", "isOver18", "rrn_sealed", "accountHolderName"],
  "nonce": "8472",
  "expiresAt": "2026-09-20T14:05:00Z"
}
```

- `purpose`는 지갑 승인 화면에 그대로 표시 (왜 달라는지 보여주기)
- **`requested`를 읽어 승인 화면을 동적으로 그릴 것.** 하드코딩 금지 — 사장님이 주소를 체크하면 지갑 화면도 바뀌어야 함 (심사위원이 확인할 수 있음)

### 제시 응답 (Holder → Verifier)

```
<본체 JWT>~<조각: name>~<조각: isOver18>~<KB-JWT>
```

- 안 보낼 조각은 제외. 본체는 손대지 않으므로 Issuer 서명 유효
- KB-JWT: `{ nonce, aud: verifier, iat }`에 홀더 개인키로 서명

---

## 8. 암호 설계

### (1) SD-JWT — 선택적 공개

원리: 항목마다 `[salt, key, value]` 조각을 만들고 **그 해시만** 본체에 넣어 서명.
제시할 때 조각 원본을 같이 주면 검증자가 해시 재계산으로 대조. 안 준 항목은 해시만 남아 내용도 의미도 알 수 없음.
salt가 필수인 이유: `isOver18`처럼 값의 경우의 수가 적으면 대입으로 맞힐 수 있음.

라이브러리: `@sd-jwt/sd-jwt-vc`. 직접 구현해도 반나절 (jose + SHA-256).

### (2) JWE — 봉인 (우리 기여)

주민번호를 국세청 공개키로 암호화해 VC의 평문 필드에 넣는다.

- 알고리즘: `ECDH-ES+A256KW` + `A256GCM` (jose의 `CompactEncrypt`)
- 사장님은 국세청 개인키가 없어 복호화 실패
- **"안 보내기"와 "잠가서 보내기"는 다르다:**

| | 주소 | 주민번호 |
|---|---|---|
| 방식 | 조각을 안 보냄 | 잠가서 보냄 |
| 사장님 | 존재도 모름 | 갖고 있지만 못 엶 |
| 국세청 | 안 감 | 열어서 봄 |

### (3) Holder Binding — 재사용 방지

VC의 `cnf`에 홀더 공개키를 박아둔다. 제시할 때 검증자가 준 `nonce`에 홀더 개인키로 서명(KB-JWT). 다른 검증자는 다른 nonce를 주므로 사장님이 받은 걸 복사해도 통과 불가.

### (4) 폐기 — 온체인

VC의 `statusIndex`를 컨트랙트에서 조회. **인덱스는 난수 배정** (순차 배정하면 발급 순서·시점이 드러남).

---

## 9. 온체인 — 블록체인을 쓰는 이유

### 왜 체인인가 (발표 답변)

> "폐기 확인을 발급기관에 물어보면, 발급기관이 '이 사람이 지금 어디서 신분증을 쓰는지' 알게 됩니다. 그걸 막으려고 씁니다."

- 발급자-검증자 비연결성(unlinkability)이 목적
- 체인에 올라가는 것: **인덱스 번호와 폐기 여부뿐. 개인정보 0**
- 인덱스↔사람 매핑은 발급기관만 안다. 주민센터 DB를 **대체하지 않는다**

### 컨트랙트

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.24;

contract IssuerRegistry {
    address public issuer;
    mapping(uint256 => bool) public revoked;

    event Revoked(uint256 indexed index, uint256 timestamp);

    constructor() { issuer = msg.sender; }

    function revoke(uint256 index) external {
        require(msg.sender == issuer, "not issuer");
        revoked[index] = true;
        emit Revoked(index, block.timestamp);
    }
}
```

- 로컬 anvil로 개발 → Sepolia 배포 후 주소·익스플로러 링크를 README에 명시
- 매핑(A안) 사용. 비트맵 최적화는 시간상 생략

### 선택: 발급기관 공개키 레지스트리 (여유 있으면)

같은 컨트랙트에 국세청·주민센터 공개키 이력을 추가할 수 있다. 목적은 **키 교체를 몰래 못 하게** 하는 것 (홈페이지 바꿔치기 방어). 다만 컨트랙트 소유자 키가 털리면 동일하게 뚫리며, 보호의 정체는 "차단"이 아니라 **"몰래 못 함"**이다. 과장 금지. **우선순위 낮음 — 시간 남으면.**

---

## 10. UI 명세

### 레이아웃

```
┌──────────────────────────────────────────────┐
│ ●주민센터(Issuer) ─ ○나(Holder) ─ ○사장님(Verifier) ─ ○국세청   [리셋] │
├──────────────────────────────────────────────┤
│                                              │
│          [모달 1개 또는 2개]                    │
│           배경은 흐리게(blur)                   │
│                                              │
├──────────────────────────────────────────────┤
│ ⛓ ON-CHAIN   Sepolia · IssuerRegistry        │
│   isRevoked(4821) → false        (읽기)       │
│   revoke(4821) → 0x7a3f...  ⏳→✓  (쓰기)      │
│   체인 기록: 인덱스와 폐기 여부뿐 · 개인정보 없음  │
└──────────────────────────────────────────────┘
```

- 상단: 현재 활성 주체만 밝게, 지나간 건 체크, 앞의 것은 흐리게. **클릭하면 해당 단계로 점프** (심사위원이 원하는 것만 볼 수 있게)
- 모달: 브라우저 창 느낌의 타이틀 바 + 주체 이름. 1개면 가운데, 2개면 좌우
- 하단 체인 바: 항상 보임. 읽기는 회색, 쓰기는 강조 + 트랜잭션 해시 링크

### 모달 2개를 띄우는 두 장면 (핵심)

**장면 A — 제시**
```
[사장님] 요청 항목 체크 + QR 표시     [나] 승인 화면 + [스캔하기 ▸] 버튼
```
QR은 표시만 하고 **[스캔하기] 버튼 클릭으로 대체**. 실제 스캔 대신 버튼. 발표에서 "실제로는 QR·딥링크로 연결됩니다" 한 줄.

**장면 B — 봉인 (프로젝트의 클라이맥스)**
```
[사장님]                        [국세청]
🔒 세무신고용 봉인               🔓 010314-3xxxxxx
[열기] → ❌ 복호화 실패           지급명세서 자동 작성
```
같은 데이터, 다른 결과. **이 장면 캡처 한 장이 프로젝트 설명이 된다.**

### 지갑 승인 화면 (= 필수 제출물 "공개 범위 인터페이스")

```
행사운영팀
일당 지급 및 원천징수 신고
─────────────────────────
보여줄 정보
  ✓ 성명 · 윤태호
  ✓ 만 18세 이상 · 예

잠긴 채 전달 (상대는 열 수 없음)
  🔒 주민등록번호 — 국세청만 열 수 있음

보내지 않음
  ✗ 주소   ✗ 사진   ✗ 발급일자
─────────────────────────
        [거부]    [제시하기]
```

**3칸 구분(보여줌 / 잠김 / 안 보냄)이 이 프로젝트의 얼굴.** 발표 자료에도 크게 넣을 것.

**과잉 요청 경고** (구현 10분, 인상 큼):
```
⚠ 일당 지급에는 주소가 필요하지 않습니다
```
용도별 필요 항목 목록을 상수로 두고 비교.

### 사장님 검증 결과 화면

```
✅ 주민센터 서명 확인
✅ 유효기간 정상
✅ 본인 제시 확인 (nonce 8472)
✅ 폐기 여부 확인 — 체인 조회 완료
─────────────────────
성명            윤태호
만 18세 이상     예
예금주 일치       ✅
🔒 세무신고용     [열어보기]
```

### 톤

검정 배경 + 모노스페이스 라벨 + 얇은 패널 테두리. 주체별 액센트 색 하나씩(주민센터 파랑 / 나 초록 / 사장님 주황 / 국세청 회색).

### 디자인 레퍼런스

![모달 프레임 레퍼런스](./assets/design-ref-window-frame.png)

위 이미지(Awwwards split-screen 카테고리의 C-Screens)에서 가져올 것은 **모달의 프레임 처리**다.

- 콘텐츠가 **독립된 브라우저 창 안에 담긴 것처럼** 보이는 프레임. 라운드 코너, 얇은 밝은 테두리, 바깥쪽에 넓은 여백
- 프레임 **바깥 배경은 흐리게(blur) 처리**되어 창이 떠 있는 느낌
- 프레임 상단에 얇은 반투명 바 → 여기에 주체 이름을 넣는다 (`🏪 사장님 화면`)
- 액센트 컬러는 **원형 버튼 하나에만** 강하게 (이미지의 마젠타 원형 버튼처럼). 우리 화면에서는 `[스캔하기 ▸]`와 `[제시하기]`에 사용

이 처리를 쓰는 이유: 모달 하나하나가 "그 주체의 화면"이라는 게 설명 없이 전달된다. 모달 2개를 나란히 띄우는 장면(§10 장면 A·B)에서 특히 효과가 크다.

**가져오지 않을 것**: 풀블리드 사진 배경, 대형 타이포 오버레이. 우리는 정보 밀도가 높은 관제 화면이므로 사진 배경은 쓰지 않는다.

색·타이포 톤은 Awwwards SOTD "USAvionix"(basement studio, 흑백 2색, 기술적 관제 톤) 쪽을 따른다.

---

## 11. 시연 시나리오 (7단계)

| # | 활성 | 모달 | 체인 |
|---|---|---|---|
| 1 | 주민센터 | 발급 폼 → 발급 | — |
| 2 | 나 | 신분증 카드 저장됨 | — |
| 3 | 사장님 + 나 | **2개** — 요청/QR ↔ 승인 화면 | — |
| 4 | 사장님 | 검사 4개 통과 + 결과 | `isRevoked` 읽기 |
| 5 | 사장님 + 국세청 | **2개** — 열기 실패 ↔ 열기 성공 | — |
| 6 | 주민센터 | 분실 신고 | `revoke` 쓰기 ⏳→✓ |
| 7 | 사장님 | 같은 VC 재제시 → **거부** | `isRevoked` → true |

**주민등록증 촬영/업로드는 절대 넣지 말 것.** 시연 시작이 "신분증 사진 올리기"면 문제 정의가 무너진다. 주민센터 발급 폼에 직접 입력하고, "이 정보는 이미 주민센터 DB에 있는 것"이라고 한 줄 설명.

재발급은 구현 안 함. 폐기가 가점 항목이지 재발급은 아님.

---

## 12. 공격 시연 (검증 가능성 항목)

| 공격 | 방법 | 결과 |
|---|---|---|
| 서명 위조 | 본체 JWT 한 글자 변조 | ❌ 서명 불일치 |
| 값 변조 | 조각의 value 변경 | ❌ 해시 불일치 |
| 만료 | exp 지난 VC | ❌ 만료 |
| **재사용** | 사장님이 받은 VP를 다른 검증자 창에 붙여넣기 | ❌ nonce 불일치 |
| **폐기** | 분실 신고 후 재제시 | ❌ 온체인 폐기됨 |
| **봉인 탈취** | 봉인만 빼내 다른 곳에서 열기 시도 | ❌ 복호화 실패 |

**탐지 못 하는 것도 같은 표에 넣을 것** (정직성이 점수가 됨):
- 사장님이 알게 된 "이름"을 메모해두는 것 → 기술로 못 막음. 대신 넘어가는 양이 최소
- 검증자와 제3자가 실시간 중계하는 공격 → `aud`(검증자 신원) 서명으로 완화만 가능

---

## 13. 기술 스택

| 영역 | 선택 |
|---|---|
| 언어/프레임워크 | TypeScript + Vite + React + Tailwind |
| 키·DID | `did:key` + Ed25519 (`@noble/curves`) |
| JWT/JWE | `jose` |
| 선택적 공개 | `@sd-jwt/sd-jwt-vc` |
| 지갑 저장소 | IndexedDB (`idb-keyval`) |
| 체인 | Foundry(anvil→Sepolia) + `viem` |
| QR | `qrcode` (표시만, 스캔은 버튼) |
| 세션 서버 | Express 또는 Vite 미들웨어 + 메모리 Map |

### 세션 API (지갑↔검증자 전달)

```
POST /session          요청 생성 → { sessionId }
GET  /session/:id      지갑이 요청 조회 / 사장님이 응답 폴링
POST /session/:id/vp   지갑이 응답 전송
```

- 메모리에만, TTL 5분, 검증 후 삭제
- **README에 명시**: "시연 편의를 위한 세션 서버. 실제로는 QR·딥링크로 지갑과 검증자가 직접 통신"

### 레포 구조 (역할 구분이 코드에서 드러나야 함)

```
src/
  issuer/      주민센터·은행 — 발급 키, 발급 로직
  holder/      지갑 — 홀더 키, IndexedDB, 제시 로직
  verifier/    사장님 — 검증 로직 (5가지 검사)
  tax/         국세청 — 복호화 키
  shared/      타입, crypto 유틸, 스키마
  ui/          모달, 상단 진행바, 체인 바
contracts/     IssuerRegistry.sol
```

> 발표 문장: "한 화면이지만 각 역할은 자기 키만 갖고 서로의 비밀에 접근하지 않습니다."

---

## 14. 구현 순서

날짜로 나누지 않는다. **앞 단계가 끝나면 바로 다음으로 넘어간다.**
각 단계는 그 자체로 동작해야 하고, 여기서 멈춰도 제출 가능한 상태여야 한다.

### Phase 1 — 뼈대와 키 (완료 조건: 콘솔에서 발급→검증 왕복 1회 통과)
- 레포 세팅, §13 폴더 구조
- Ed25519 키쌍 생성, `did:key` 인코딩
- 신분 VC 발급 (SD-JWT), 검증자 측 서명·해시 대조
- 아직 UI 없음. 스크립트로 통과 확인

### Phase 2 — 선택적 공개 (완료 조건: 조각 일부만 보내도 검증 통과)
- 제시 시 `disclose` 배열로 조각 선택
- 안 보낸 항목이 검증자 쪽에서 해시로만 남는지 확인
- 요청 JSON / 응답 포맷 확정

### Phase 3 — Holder Binding (완료 조건: 남의 VP 재사용이 거부됨)
- `cnf` 필드, KB-JWT 서명·검증
- nonce 불일치 시 거부되는 테스트

### Phase 4 — 봉인 (완료 조건: 사장님은 실패하고 국세청은 성공)
- 국세청 키쌍, JWE 암호화
- 발급 시 `rrn_sealed` 생성
- 복호화 시도 2개 경로 (실패/성공)
- **여기까지가 프로젝트의 핵심.** 이 단계가 끝나면 기능은 완성

### Phase 5 — UI (완료 조건: 시연 7단계가 화면으로 돌아감)
- 상단 진행 라인, 모달 1개/2개 전환
- 지갑 승인 화면 3칸 (보여줌 / 잠김 / 안 보냄)
- 사장님 검증 결과 화면
- 하단 체인 바 (아직 더미 값)

### Phase 6 — 온체인 폐기 (완료 조건: 분실 신고 후 재제시가 거부됨)
- `IssuerRegistry.sol`, anvil에서 테스트
- 검증 시 `isRevoked` 조회 연동
- Sepolia 배포, 주소·익스플로러 링크 확보
- 체인 바에 실제 트랜잭션 표시

### Phase 7 — 계좌 VC (완료 조건: 예금주 일치 판정이 뜸)
- 은행 Issuer 추가
- 두 VC 성명 대조 → "통장 사본도 불필요" 논거 완성

### Phase 8 — 공격 시연 (완료 조건: §12 표의 6개가 화면에서 재현됨)
- 시나리오 버튼으로 각 공격 실행
- 탐지 못 하는 항목도 같은 표에 표시

### Phase 9 — 마감 작업
- README (§15 한계 전부 포함, AI 사용 명시)
- 공개 범위 비교표 (지금 넘어가는 항목 vs 우리 방식)
- 시연 영상
- 제출

### 잘라낼 수 있는 것

시간이 밀리면 Phase 7 → 6 → 8 순으로 줄인다.
**Phase 1~5까지만 있어도 평가 기준의 "최소 한 경로 동작"은 충족한다.**
과잉 요청 경고와 공개키 레지스트리(§9 선택 항목)는 여유가 있을 때만.

---

## 15. README에 반드시 명시할 한계

1. 현행 소득세법상 지급명세서에 주민번호 기재가 필요하므로, **번호를 없애는 게 아니라 사업주를 거치지 않게 하는 것**
2. 실제 동작하려면 홈택스가 봉인 포맷을 수용해야 함. 프로토타입은 국세청 목업
3. 세무조사 증빙으로서의 법적 인정은 별도 문제
4. 도입 주체가 소상공인이라 확산 부담이 큼
5. 선택적 공개는 EU 표준(EUDI/SD-JWT)의 기본 기능이며 본 프로젝트의 발명이 아님. 기여는 봉인 필드
6. 세션 서버는 시연 편의용
7. RPC 제공자는 어느 인덱스를 조회했는지 볼 수 있음 (완전한 조회 프라이버시는 아님)
8. AI 사용 내역 명시

---

## 16. 발표 Q&A 대비

**Q. 굳이 블록체인인가?**
> 폐기 확인을 발급기관에 물어보면 발급기관이 사용 이력을 알게 됩니다. 그걸 막으려고 씁니다. 체인에는 인덱스와 폐기 여부만 올라가고 개인정보는 한 글자도 없습니다.

**Q. VC가 Web3 기술인가?**
> VC·DID는 W3C 웹 표준입니다. EU 디지털 신원 지갑도 체인을 쓰지 않습니다. 저희가 체인을 쓴 곳은 폐기 한 군데입니다. (※ "VC는 원래 Web3 기술"이라고 답하면 안 됨)

**Q. 이미 있는 거 아닌가? (캐치시큐 등)**
> 그건 사본을 **안전하게 받는** 서비스입니다. 원본은 여전히 존재하고 위치만 옮깁니다. 저희는 사본을 **만들지 않습니다**.

**Q. 사장님이 받은 걸로 도용하면?**
> 다른 곳에서는 nonce가 달라 통과되지 않습니다. 주민번호는 봉인돼 있어 유출돼도 열 수 없습니다. 다만 알게 된 "이름"을 메모하는 건 못 막습니다 — 대신 넘어가는 양이 이름 하나뿐입니다.

**Q. 실무자는 사본을 세무 증빙으로 원한다.**
> 맞습니다. 서명된 VC가 그 역할을 하려면 세무 당국의 인정이 필요하고, 지금은 근거가 없습니다. 한계로 명시했습니다.

**Q. 블록체인에 개인정보 올리는 거 아닌가?**
> 인덱스 번호와 폐기 여부뿐입니다. 그 번호가 누구인지는 발급기관만 압니다. 개인정보를 체인에 올리면 파기 의무와 충돌하므로 의도적으로 배제했습니다.

---

## 17. 폐기된 대안 (다시 꺼내지 말 것)

- **트랙 03 (오프체인 의사결정 검증)**: 같은 설계의 타 팀 공개 저장소 존재(`kimsabin725/blocknotice`, `tnwjd023-boop/BlockNotice_GoldRWA`). 결과물이 "증거"에 그쳐 체감 임팩트 낮음
- **트랙 01**: 문제 정의 25점을 벌 여지 없음, 대부분 Slither 기반으로 수렴
- **트랙 04**: PoC 실행 실패 시 0점, 2.5일에 위험
- **전자출결·익명투표·배지**: 트랙 예시 그대로라 독창성 0
- **주민등록증 촬영 업로드 시연**: 문제 정의 자기모순
- **영지식 증명**: 불필요. 선택적 공개로 충분
- **OpenID4VP/OID4VCI 프로토콜 구현**: 시간 초과. 포맷 호환만
- **모바일 앱**: 전부 웹
- **4분할 동시 표시**: 모달 방식으로 대체 (대비 장면이 안 살아남)

---

## 18. Blockchain Valley 관련

Ray는 공동 주최사인 Blockchain Valley의 개발 리드. 규정에 주최 측 소속 참가 제한 조항은 없으나, 참가 가능 여부를 디스코드 또는 dev@anam145.com으로 확인해 둘 것.
