# 국세청 (tax)

Issuer 도 Verifier 도 아니다. **봉인 수신자**다. 검증하지 않고 복호화만 한다. (§6)

국세청이 검증까지 하면 국세청이 "이 사람이 언제 어디서 일했는지"를 알게 되어
구조가 무너진다. 그리고 사장님은 그 자리에서 판단해야 하고, 신고는 다음 달이다.

- Phase 4 에서 채운다: 국세청 키쌍, `rrn_sealed` JWE 복호화 (`ECDH-ES+A256KW` + `A256GCM`)
